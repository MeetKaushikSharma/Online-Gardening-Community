spring.application.name=gardening-community

# Server Configuration - Use PORT environment variable if available
server.port=${PORT:8080}

# Database Configuration - Use environment variables for deployment
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://sql12.freesqldatabase.com:3306/sql12809214}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:sql12809214}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:TnWjtUhaKQ}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# CORS Configuration
server.error.include-message=always
server.error.include-binding-errors=always

# Logging
logging.level.root=INFO
logging.level.com.gardening=${LOG_LEVEL:INFO}
